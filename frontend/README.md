# To-Do List Manager Frontend

React app for managing tasks. Communicates with backend API at `/api/tasks`.

## Available Scripts
- `npm start` — Runs the app in development mode
- `npm run build` — Builds the app for production
